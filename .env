# Required: shared secret required in header "x-bridge-key"
BRIDGE_KEY=7sxc78s87tdfc87dstfcv7sdvc78svcvs7vzchalc87

# Optional: override target API (default points to your RDP)
TARGET_API=http://194.59.31.166:5127

# Optional: restrict CORS to your domains (comma-separated). If empty, allows all.
# ALLOWED_ORIGINS=

# Optional: proxy log level (debug|info|warn|error|silent)
# PROXY_LOG_LEVEL=warn
