{
  "name": "render-http-bridge",
  "version": "1.0.0",
  "description": "HTTPS to HTTP bridge (Render) forwarding to an HTTP API (RDP).",
  "main": "index.js",
  "type": "commonjs",
  "scripts": {
    "start": "node index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "http-proxy-middleware": "^2.0.6"
  }
}